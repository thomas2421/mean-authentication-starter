<h1 class="text-center">{{username}}'S WORKOUT</h1>
<div *ngFor="let exercise of exercises">
  <div class="row">
    <div class="text-center main">
      <a class="btn btn-info main" (click)=navigate(exercise)>{{exercise}}</a>
    </div>
  </div>
</div>
